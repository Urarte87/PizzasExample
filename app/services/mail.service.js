function sendEmail(pizzas, totalPrice) {
	//console.log(pizzas);
	console.log("Enviando correo electronico de confirmacion a " + pizzas.userId + " precio final: " + totalPrice + " euros");
}

module.exports = {
  sendEmail
}